extends layout

block content
  #recorder.row
    #overlay
      img(src='/images/curtains.png')
    .video
      img#gif.show(src=gif.get('url'))

  ul#gifs.row

  script!= "bs.Gifs = new bs.collections.Gifs(" + stringifiedGifs + ");"
