extends layout

block content
  #recorder.row
    video(autoplay=true)
    canvas
    img#gif
    button(id="record") Record
    button(id="stop") ◼

  ul#gifs
    - each gif in gifs
      li 
        img(src="#{gif}")
