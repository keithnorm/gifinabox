extends layout

block content
  #recorder.row
    .video
      video(autoplay=true)
      .buttons
        button#record Record
        button#stop ◼
    canvas
    img#gif
    input#link(type="text")

  ul#gifs.row
    - each gif in gifs
      li
        a(href=gif.link())
          img(src="#{ gif.get('encodedData') }")
